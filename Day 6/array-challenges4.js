// Declare an array of cities which contain "Mumbai", "Sydney", and "Berlin".
// Add "Berlin" to the end of the array and log the updated array using push method.
let citiesVisited = ["Mumbai","Sydney"];
citiesVisited.push("Berlin"); // Can use citiesVisited[citiesVisited.length] = "Berlin"; // but push is more concise.
console.log(citiesVisited);
