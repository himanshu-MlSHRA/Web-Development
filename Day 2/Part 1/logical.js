// && -> and
// || -> or
// ! -> reverse (not)

let isLoggedIn = true;
let isPaid = false;

console.log(isLoggedIn && isPaid); // false (both must be true)

let isEmailUser = true;
let isGoogleUser = false;

console.log(isEmailUser || isGoogleUser); // true (any one should be true)
