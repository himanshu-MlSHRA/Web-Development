// Check if a variable is Number or not

let num1 = 20; // This is a number
let num2 = "20"; 
let num3 = [20, 30, 40]; // This is an array, not a number
if (typeof score === "number") { //if we want to use typeof use 3 ='s 
    console.log("Variable is a number");
    
} else {
    console.log("Variable is not a number");
    
}